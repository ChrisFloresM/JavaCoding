public class StringsExc {

    public static void main(String[] args) {

        //Methods:

        //.trim()
        //.isEmpty() JDK 6+
        //.isBlank() JDK 11+
        //.length()
        //.toUpperCase()
        //.toLowerCase()
        //.equals(Str)
        //.equalsIgnoreCase(Strings)
        //.compareTo(Str)
        //.charAt(int);
        //.substring(int init, init end) - inclusive, exclusive
        //.replace(char old, char new);
        //.indexOf(char find);
        //.lastIndexOf(char find);
        //.contains(sbstring);
        //.startsWith(sbstring);
        //.endsWith(sbstring);
        //.split(regex "");

    }
}
